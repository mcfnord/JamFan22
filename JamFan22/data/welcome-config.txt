# Silent/listener GUIDs — never mention as crew or forecast (won't appear as sound-makers)
crew_exclude_guid=6c633a5a290a8e998c8d1fb5039e2161
crew_elsewhere_min_mins=30
forecast_min_mins=60
forecast_sighting_hours=2
forecast_max_entries=4
model=gemini-2.5-flash
temperature=0.9
pro_temperature=1.3
# Pro tier — only for guests the server already knows (WelcomeContext.IsLoreGuest). All hot, no restart.
pro_model=gemini-2.5-pro
# Which sources make someone 'known': lore (this server's server-lore.json notes/themes/tagline),
# regulars (session-regulars.json for this server), list (pro_names below). `off` disables Pro entirely.
pro_gate=lore,regulars
# Extra names for the `list` source, comma-separated, exact whole-name match, any server.
pro_names=
# gemini-2.5-pro rejects 0 (HTTP 400 'only works in thinking mode'); 128 measured at 2.2-2.4 s.
pro_thinking_budget=128
# Return floor EVERY source must pass: census minutes on fleet servers (rolling window). A name on the
# list or in lore with fewer minutes than this is still a stranger. 0 disables the floor.
pro_min_fleet_minutes=120
llm_timeout_ms=6000
group_llm_timeout_ms=6000
# Dormant TH "See your destiny" joiner-centered essay (Pro model, longer timeout).
# Feature also self-expires: hardcoded 2026-08-17 conclusion OR 29 essays produced (RAM), whichever first.
essay_model=gemini-2.5-pro
essay_llm_timeout_ms=20000
# Song-artist clues from url-guids.csv: a chart counts only after this many DISTINCT DATES with the player
# present (operator: one session is not a signal). Line omitted when nothing qualifies.
charts_min_dates=2
